{
    "name": "likewinter/card-deck",
    "description": "Card deck games library",
    "type": "library",
    "require": {
        "php": "^8.5"
    },
    "require-dev": {
        "phpstan/phpstan": "^2.0",
        "pestphp/pest": "^4.2",
        "carthage-software/mago": "^1.45"
    },
    "license": "MIT",
    "autoload": {
        "psr-4": {
            "Likewinter\\CardDeck\\": "src/"
        }
    },
    "authors": [
        {
            "name": "Anton Orlov",
            "email": "anton@pinked.ru"
        }
    ],
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "scripts": {
        "test": ["Composer\\Config::disableProcessTimeout", "pest"],
        "phpstan": "phpstan analyse --memory-limit=1G",
        "audit": "composer audit --format=plain",
        "coverage": ["Composer\\Config::disableProcessTimeout", "pest --coverage"],
        "lint": "mago lint",
        "analyze": "mago analyze",
        "format": "mago format",
        "format:check": "mago format --check",
        "api-docs": "php scripts/generate-api-reference.php",
        "api-docs:check": "php scripts/generate-api-reference.php --check",
        "hooks": "git config core.hooksPath .githooks",
        "ci": [
            "@audit",
            "@phpstan",
            "@lint",
            "@analyze",
            "@format:check",
            "@api-docs:check",
            "@test"
        ]
    },
    "scripts-descriptions": {
        "test": "Run the Pest test suite",
        "phpstan": "Run PHPStan static analysis at level 8",
        "audit": "Check dependencies for known security vulnerabilities",
        "coverage": "Run tests with PCOV coverage report",
        "lint": "Run Mago linter",
        "analyze": "Run Mago static analyzer",
        "format": "Format PHP source with Mago",
        "format:check": "Check formatting without modifying files",
        "api-docs": "Regenerate docs/api.md from the docblocks in src/",
        "api-docs:check": "Fail if docs/api.md is out of date with the docblocks in src/",
        "hooks": "Point git hooks at .githooks/ (run once after cloning)",
        "ci": "Run all CI checks (audit + phpstan + lint + analyze + format + api-docs + tests)"
    }
}
